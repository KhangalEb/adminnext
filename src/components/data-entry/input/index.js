import React from 'react';
import { Input } from 'antd';
const Index = ({ placeholder, defaultValue }) => {
  return (
    <Input placeholder={placeholder} defaultValue={defaultValue} />
  );
}

export default Index;
